# RealProjectDevops

Ich werde ein Spiel bauen, mit dem ich lernen und trainieren kann. die Datein werden mit Dockerfile in Image gebaut und in Registry hochgeladen. Danach wird das mit Docker ausgeführt und mit Docker-Compose getestet, und dann in k8s deployieren 